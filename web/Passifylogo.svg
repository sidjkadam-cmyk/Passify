<svg width="120" height="120" viewBox="0 0 120 120" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Teal ticket shape with rounded corners -->
  <rect x="25" y="20" width="70" height="80" rx="6" ry="6" fill="#06b6d4"/>
  
  <!-- Left inward-curving indentation (pinched) -->
  <ellipse cx="25" cy="60" rx="8" ry="20" fill="#0f172a"/>
  
  <!-- Right inward-curving indentation (pinched) -->
  <ellipse cx="95" cy="60" rx="8" ry="20" fill="#0f172a"/>
  
  <!-- Top triangle pointing up (black) -->
  <path d="M 50 40 L 60 30 L 70 40 Z" fill="#000000"/>
  
  <!-- Bottom triangle pointing down (black) -->
  <path d="M 50 80 L 60 90 L 70 80 Z" fill="#000000"/>
  
  <!-- Horizontal diamond/rhombus gap between triangles (negative space) -->
  <path d="M 55 55 L 60 60 L 65 55 L 60 50 Z" fill="#06b6d4"/>
</svg>
